<h1>Users#index</h1>
<% @users.each do |user| %>
    <%= user.email %><br/>
    <%= user.avatar %><br/>
    <td><%= image_tag user.avatar, width: "50px" if user.avatar.attached? %></td>
<% end %>
<%# <%= image_tag(@project.photos.image) %>
