<!DOCTYPE html>
<html lang="en">
<head>
  <title>Bootstrap 4 Website Example</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
  <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.1/dist/jquery.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
  <style>
  .fakeimg {
    height: 200px;
    background: #aaa;
  }
  </style>
</head>
<body class= "project-body">
  <% project = Project.first %>
    <div id="carouselExampleDark" class="carousel carousel-dark slide" data-bs-ride="carousel">
        <div class="carousel-indicators">
            <% @project.images.each.with_index do |image, i| %>
              <button type="button" data-bs-target="#carouselExampleDark" data-bs-slide-to="<%= i %>" class="<%= 'active' if i == 0%>"></button>
            <% end %>
        </div>

        <div class="carousel-inner">
          <% @project.images.each.with_index do |image, i| %>
            <div div class="carousel-item active">
              <%= image_tag(image, alt: SecureRandom.uuid) %>
            </div>
          <% end %>
        </div>
      <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleDark" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
      </button>
      <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleDark" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
      </button>
  <% if false %>
  <%= yield %>
  <% end %>


  </div>
  <div class="project-title"></div>
  <div class="descriptions-container">
    <div class="row">
      <div class="col-sm-8">
        <h1 class="project-title"><%= @project.title %></h1>
        <div class="project-desc">
          <p><%= @project.description %></p>
          <h2>Le Projet Comporte</h2>
          <p><%= @project.description %></p>
          <hr class="d-sm-none">
         </div>

      </div>
      <div class="col-sm-4">
        <%= image_tag("pdf_icon.png", width:60)%>
        <%= image_tag("pdf_icon.png", width:60)%>
        <%= image_tag("3D_icon.png", width:60)%>
        <br>
        <%= image_tag("logoifc.png", width: 300)%>
        <div class="project-infos">
          <p>Project Owner</p>
          <h3><%= @project.owner %></h3>
          <br>
          <p>Contractor</p>
          <h3><%= @project.main_contractor %></h3>
          <br>
          <p>Duration of the project</p>
          <h3><%= @project.duration %></h3>
          <br>
          <p>Total Budget</p>
          <h3><%= @project.budget %></h3>
          <br>
          <p>Labor Force</p>
          <h3><%= @project.labor_force %></h3>
          <br>
          <p>Longitude & Latitude</p>
          <h3>long - <%= @project.latitude %> lat - <%= @project.longitude %></h3>
          <iframe src=<%= "http://127.0.0.1:3000/projects" %> width="100%" height="500px"></iframe>
        </div>
      </div>
    </div>
  </div>
  <div class="back-btn-project">
      <%= link_to "Go to my projects", my_projects_path, class: "btn btn-primary" %>
      <%= link_to "Go to all projects", projects_path, class: "btn btn-primary" %>
    </div>
</body>
</html>
